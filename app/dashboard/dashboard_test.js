'use strict';

describe('ROAnalyzer.dashboard module', function() {

  beforeEach(module('ROAnalyzer.dashboard'));

  describe('dash controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('dashCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});