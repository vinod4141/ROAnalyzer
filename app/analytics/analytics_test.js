'use strict';

describe('ROAnalyzer.analytics module', function() {

  beforeEach(module('ROAnalyzer.analytics'));

  describe('Analytics controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('anCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});